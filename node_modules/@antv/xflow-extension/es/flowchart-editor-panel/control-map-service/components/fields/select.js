import React from 'react';
import { Select } from 'antd';
import { FormItemHeight } from '../constants';
const { Option } = Select;
const SelectField = props => {
    const { label = '箭头', value, onChange, options = [], width } = props;
    return (React.createElement("div", { className: "group" },
        React.createElement("label", null, label),
        React.createElement(Select, { size: "small", value: value, style: {
                width,
                height: FormItemHeight,
            }, getPopupContainer: trigger => trigger.parentNode, optionFilterProp: "children", onChange: (v) => {
                onChange === null || onChange === void 0 ? void 0 : onChange(v);
            }, filterOption: (input, option) => (option === null || option === void 0 ? void 0 : option.children.toLowerCase().indexOf(input.toLowerCase())) >= 0 }, options.map(option => (React.createElement(Option, { key: option.label, value: option.value }, option.label))))));
};
export default SelectField;
//# sourceMappingURL=select.js.map