import { PathLayer } from '../../layers/path-layer';
import { PlotLayerOptions } from '../../types';
export declare const createCountryBoundaryLayer: (data: any, plotLayerConfig?: PlotLayerOptions | undefined) => {
    chinaBoundaryLayer: PathLayer;
    chinaDisputeBoundaryLayer: PathLayer;
};
